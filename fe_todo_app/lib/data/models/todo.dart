class Todo {
  String? docId;
  String title;
  String todoTask;
  String todoDescription;
  bool isCompleted;

  Todo({
    required this.title,
    required this.todoTask,
    required this.todoDescription,
    required this.isCompleted,
  });
}
